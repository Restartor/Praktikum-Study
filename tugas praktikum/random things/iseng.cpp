// make ascending countdown from input number
#include <iostream>
#define ascending_countdown for (int i = 1; i <= n; i++) {std::cout << i << " ";}
#define descending_countdown for (int i = n; i >= 1; i--) {std::cout << i << " ";}
using namespace std;

int main() {
    int n;
    cout << " enter a number: ";
    cin >> n;
    descending_countdown

    return 0;
}